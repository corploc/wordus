name: Version Bump

on:
  workflow_dispatch:
    inputs:
      bump_type:
        description: 'Type de bump de version'
        required: true
        type: choice
        options:
          - patch
          - minor
          - major

permissions:
  contents: write

jobs:
  bump:
    name: Bump Version
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          token: ${{ secrets.PAT_TOKEN }}
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: '22'

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 9.15.4   # match Dockerfile pin

      - name: Install dependencies (no scripts — only need node_modules for conventional-changelog)
        run: pnpm install --ignore-scripts

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

      - name: Bump version
        run: |
          pnpm version ${{ inputs.bump_type }} -m "chore(release): %s"

      - name: Push changes
        run: git push --follow-tags
