# Umami Analytics — runtime config (read at container start, not baked at build).
# Leave unset to disable analytics entirely.

NUXT_PUBLIC_UMAMI_HOST=https://analytics.minist.re
NUXT_PUBLIC_UMAMI_ID=your-umami-website-id

# App Configuration

NUXT_PUBLIC_BASE_URL=https://wordus.xyz
