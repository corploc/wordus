# Disable the 24h supply-chain release-age check.
# Dependabot regenerates the lockfile with fresh packages; the default 1d
# threshold rejects them in CI even though we trust the bot. Setting to 0
# means we accept any package age. Tighten back if needed.
minimum-release-age=0
